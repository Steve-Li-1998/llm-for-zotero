# Provider logos

These SVGs are vendored from `@lobehub/icons-static-svg` version `1.91.0` (MIT), served by jsDelivr.
The upstream copyright and license are in [LICENSE](LICENSE).
Provider names and logos remain trademarks of their respective owners.

Local filenames follow the provider preset identifiers.
Upstream filenames differ for `anthropic` (`claude-color.svg`), `glm` (`zai.svg`), `mimo` (`xiaomimimo.svg`), and `copilot` (`githubcopilot.svg`).
Gemini, MiniMax, DeepSeek, Qwen, and Kimi use the upstream `-color.svg` variants.
The remaining files use the upstream provider name without a suffix.

Logos are bundled with the extension, so opening preferences does not request remote images.
Monochrome logos use the native text color through CSS masks; multicolor logos retain their artwork.
Kimi's white artwork uses a dark backing in both themes.
